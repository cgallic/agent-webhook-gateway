"""
Demo package — self-contained sample jobs for the public demo path.

Nothing in here touches the network, secrets, or any real client data.
Everything is mock/fixture data so the repo's async job flow can be
exercised end-to-end without configuration.
"""
